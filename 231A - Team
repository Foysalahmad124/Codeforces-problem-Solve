#include <iostream>
using namespace std;

int main() {
  int n, count=0;
  std::cin >> n;
  
  while(n--){
  int p,v,t,sum;
  cin>>p>>v>>t;
  sum=p + v + t;
  if(p + v + t >= 2) 
  count++;
  }
  std::cout << count << std::endl;

    
}
